<div class="form-group nested-fields">

<%= f.hidden_field :start_time, value: Time.now.strftime("%l:%M %p"), readonly: true %>

    <%= f.collection_select(:task_id, Task.where(status: "active"), :id, :title, :include_blank => "Task Select") %>	
    <%= f.collection_select(:job_id, Job.where(status: "active"), :id, :job_name, :include_blank => "Job Select") %>	
</div>
